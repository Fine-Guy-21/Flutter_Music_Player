import 'package:flutter/material.dart';

class GenresContainer extends StatelessWidget {
  const GenresContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Center(
        child: Text('Genres'),
      ),
    );
  }
}
